<list-item-wrap>
  <view class="list-item-wrap {{title ? 'with-title' : 'no-title'}}">
    <view class="title" wx:if="{{title}}">{{title}}</view>
    <view class="content {{color}}"><slot></slot></view>
  </view>
</list-item-wrap>
